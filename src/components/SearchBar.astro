---
import "../custom/theme.css";

interface Props {
  placeholder?: string;
}

const { placeholder = "Search..." } = Astro.props;
---

<div class="mb-8">
  <div class="relative">
    <input
      type="text"
      placeholder={placeholder}
      class="search-bar-input w-full px-4 py-3 pl-12 pr-4 rounded-2xl shadow-lg focus:outline-none focus:ring-2"
      id="search-input"
    />
    <div
      class="search-bar-icon absolute left-4 top-1/2 transform -translate-y-1/2"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="h-5 w-5"
        viewBox="0 0 20 20"
        fill="currentColor"
      >
        <path
          fill-rule="evenodd"
          d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z"
          clip-rule="evenodd"></path>
      </svg>
    </div>
  </div>
</div>

<script>
  const searchInput = document.getElementById("search-input");
  if (searchInput) {
    searchInput.addEventListener("input", (e) => {
      const target = e.target as HTMLInputElement;
      if (target) {
        const event = new CustomEvent("search-input", {
          detail: { value: target.value },
        });
        document.dispatchEvent(event);
      }
    });
  }
</script>
